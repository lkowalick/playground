/ drop _

a: !10

5_a / returns a with the first 5 elements removed

a_5 / returns a with the elemener at position 5 removed

dict: `a`b`c!!3

1_dict / returns dict with the first


/ Fun little idioms

{(!x)=/:!x} /identity matrix of size x by x
{!x=/:x:!x} /identity matrix of size x by x
